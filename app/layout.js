export const metadata = {
  title: "Gol IA",
  description: "App de análise de apostas",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
